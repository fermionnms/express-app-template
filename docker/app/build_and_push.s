#!/bin/bash

#need to be logged into docker to push to fermionnms

# Please make sure everything in the docker folder is setup correctly before running this script
# Remember that this app was created from a template and you need to make sure the docker image will be tagged and published correctly
# delete these messages when you are done


# docker build -t fermionnms/{{add_name_here}} -f docker/app/Dockerfile .
# docker push fermionnms/app
